<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CAPACITOR_DEBUG</key>
	<string>$(CAPACITOR_DEBUG)</string>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
        <string>Rebirth</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!-- ActivityKit — required to start Live Activities for the rest timer. -->
	<key>NSSupportsLiveActivities</key>
	<true/>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>armv7</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>

	<!-- Camera access — the in-app fitspo burst button uses the WKWebView
	     navigator.mediaDevices.getUserMedia API to capture physique inspo
	     photos. iOS blocks mediaDevices entirely when this key is missing. -->
	<key>NSCameraUsageDescription</key>
	<string>Rebirth uses your camera to take discreet fitspo / physique inspiration photos during workouts.</string>

	<!-- Photo library add-only access — captured inspo burst shots are saved
	     to the user's Photos library so they're accessible outside the app
	     even if the upload fails. Using "AddUsageDescription" scopes this to
	     write-only; we never read the user's existing photos. -->
	<key>NSPhotoLibraryAddUsageDescription</key>
	<string>Rebirth saves the photos you capture (fitspo bursts, progress) to your Photos library so you can find them there too.</string>

	<!-- Location permissions — geofence monitoring requires Always authorization -->
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>Rebirth uses your location to detect when you arrive home so it can automatically end your workout.</string>
	<key>NSLocationAlwaysAndWhenInUseUsageDescription</key>
	<string>Rebirth monitors your location in the background to detect when you arrive home and automatically end your active workout — even when the app is closed.</string>
	<key>NSLocationAlwaysUsageDescription</key>
	<string>Rebirth monitors your location in the background to detect when you arrive home and automatically end your active workout — even when the app is closed.</string>

	<!-- Background modes required for CLRegion monitoring to survive app termination -->
	<key>UIBackgroundModes</key>
	<array>
		<string>location</string>
		<string>fetch</string>
	</array>

	<!-- Custom URL scheme — iOS 18 FitspoControlWidget taps fire
	     rebirth://burst, AppDelegate handles it to trigger the burst. -->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>app.rebirth</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>rebirth</string>
			</array>
		</dict>
	</array>

	<!-- Scene manifest — explicitly wire our SceneDelegate so URL opens via
	     scene(_:openURLContexts:) reach our code. Without this, iOS 18 may
	     auto-wrap the app in a default scene that swallows URLs. -->
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
		<key>UISceneConfigurations</key>
		<dict>
			<key>UIWindowSceneSessionRoleApplication</key>
			<array>
				<dict>
					<key>UISceneConfigurationName</key>
					<string>Default Configuration</string>
					<key>UISceneDelegateClassName</key>
					<string>App.SceneDelegate</string>
					<key>UISceneStoryboardFile</key>
					<string>Main</string>
				</dict>
			</array>
		</dict>
	</dict>
</dict>
</plist>
