appId: app.rebirth
tags:
  - keyboard
---
# Workout reps input + RIR chip strip must remain tappable when keyboard
# is up. Requires an in-progress workout with at least one set row.
# Parked until the workout-with-active-set fixture lands.
- runFlow: ../../helpers/launch.yaml
- tapOn: "Workout"
- assertVisible: "Workout"
# Once seed('workout-with-3-sets') exists in test-fixtures.ts:
#   - runFlow: { file: ../../helpers/actions/seed.yaml, env: { FIXTURE: workout-with-3-sets } }
#   - tapOn: { id: m-workout-set-reps }
#   - inputText: "10"
#   - runFlow: { file: ../../helpers/actions/assert-above-keyboard.yaml, env: { SELECTOR_TEXT: RIR } }
#   - hideKeyboard
